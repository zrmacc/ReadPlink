% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReadPlinkData.R
\name{ReadPlinkData}
\alias{ReadPlinkData}
\title{Read PLINK data with genotype matrix and metadata}
\usage{
ReadPlinkData(stem, bim_rows = NULL, fam_rows = NULL)
}
\arguments{
\item{stem}{Stem of the BED, BIM, FAM files (path without extension).}

\item{bim_rows}{Row numbers of desired SNPs in the BIM file. If \code{NULL},
all SNPs are returned.}

\item{fam_rows}{Row numbers of desired subjects in the FAM file. If
\code{NULL}, all subjects are returned.}
}
\value{
A list with components \code{geno} (numeric subject-by-SNP matrix),
  \code{bim} (BIM data with columns chromosome, id, cm, pos, a1, a2), and
  \code{fam} (FAM data with columns fid, iid, pid, mid, sex, pheno).
}
\description{
Reads the BED/BIM/FAM set and returns the genotype matrix plus full BIM and
FAM tables (optionally subset by \code{bim_rows} and \code{fam_rows}). Useful
when you need SNP/sample metadata together with genotypes.
}
